# MVP validation — 2026-09-23

Node.js 24.18.0 / Python 3 / macOS / Codex in-app Chromiumで実施。

## 複数機体UDP — 2026-09-24

- `npm test`: Zigバックエンドで76/76成功。2機への実UDP同時指令と独立したテレメトリ、片方だけのOFF、再ON時のポート維持・旧セッション拒否を確認。
- ポート競合の回避、OFF中の割り当て保持、再ON時の競合エラー、ポート枯渇、同時HTTPリクエスト、機体置換・全リセットでのソケット解放を確認。
- 専用サーバーで `tests/demo_smoke.py`、`tests/udp_smoke.py`、`tests/separation-smoke.js` が成功。既存デモの待機・起動・停止・制御権移譲、Pythonクライアントの期限・緊急停止、多段分離・上段点火を検証。
- ブラウザの機体一覧でON/OFF、2機同時ONと異なる受信・送信ポート、追加機体の初期OFF、追尾先の接続コマンド・ヘルプ表示を確認。535 px幅で一覧表示の崩れなし。console error/warningなし。
- `npm run docs:build` 成功。

## 過去の検証記録

Zig移行時の結果は [physics-kernel.md](physics-kernel.md) を参照。その時点ではZig版・JavaScript参照版ともに62テスト成功です。[completion-audit.md](completion-audit.md) と以下は実装段階ごとの記録です。

|検証|結果|
|---|---|
|`npm test`|21/21 passed|
|上流PyLoNのPythonパーサー|71テレメトリパケットを解析成功|
|上流の関数で生成したコマンド|acquire / engine / flight / RCS / wrench / release の6指令を受理|
|`python3 tests/udp_smoke.py`|実UDPで60,000 N点火、上昇、重複拒否、指令期限切れ、緊急停止を確認|
|実時間15秒のPythonデモ|約1 kmまで上昇し、終了時に推力停止・lease release|
|90秒相当の高速シミュレーション|高度42,117 m、速度1,260 m/s、迎角0.245°、計算状態はfinite|
|真空円軌道3周|エネルギー/角運動量の相対誤差 < 1e-9、半径ずれ < 0.1 m（テスト基準）|
|ブラウザ操作|パーツ追加、Undo、左右ミラー、側面位置調整、保存、初期機体復元、発射台/組立室切替、説明ダイアログを確認|
|左右ミラー角度|15° / 165°として保存されることを確認|
|ブラウザ表示|通常幅1280 pxと狭い実表示幅547 pxで確認。検証時のconsole error/warningなし|

参照PyLoN commit: `d62d948064d6665702d05957a669596244dca8da`。

上流互換試験はROS/DDSやKSPを起動せず、上流の純粋なpacket parser/encoderを使用しています。実機との空力係数照合、風洞試験、ROS全体の結合試験は行っていません。高速飛行シナリオの結果は、このMVPの近似モデルに対する動作確認です。

## UDPデモ・地球描画の追加検証

- `tests/demo.test.js`: PyLoNテレメトリだけを入力する180秒の自動操縦シナリオ。75%推力、RCSロール、燃料・電力消費、高度100 km超え、推力停止後に再点火しないことを確認。単段・2段・3段、段ごとのエンジン選択、下から順の分離、上段燃料の温存、分離確認後の点火待機、遅延・順序逆転・指令損失・分離未確認時の停止も検証。×2・×5・×10では上段の燃焼中の姿勢と高度100 km到達を確認。
- `python3 tests/demo_smoke.py [URL]`: 起動直後の機体へボタン操作なしで自動点火すること、飛行セッション不在時の待機、SIGINT時の推力OFFとlease解放、再開、外部コントローラによる制御権取得、セッション変更時の終了を確認。アプリにデモ専用API・状態がないことも検証。さらに×10で2段機体を180シミュレーション秒飛ばし、実UDP経由の燃料切れ・切り離し・上段点火・高度100 km超え・推力OFFとlease解放まで確認。起動直後の専用の確認用サーバーを使用し、終了時は×1・組立室へ復帰。
- `tests/orbit.test.js`: 円・楕円・放物線・双曲線・動径軌道、回転方向、地表衝突点を検証。弾道の終点を真空RK4積分と照合。
- 旧MVPでの検証記録（現在は独立CLIへ移行）：ブラウザ内蔵デモで実時間約160 kmまで飛行。4分でデモ終了、推力OFF、制御権解放後も弾道計算が続くことを確認。
- 海岸線テクスチャ、マップ、大気の縁、宇宙の星空、機体マーカー、追尾/全景カメラ切り替えを実表示で確認。WebGLのコンパイルエラー・console error/warningなし。
- 追加作業時の `npm test`: 28/28 passed（並行して追加された組立テストも含む）。

地球の色、雲と大気散乱の描画は視覚的な近似です。描画の見た目を物理モデルの精度検証には用いていません。

## 面への配置・分離機構の追加検証

- `npm test`: 34/34 passed。自由配置、近接点だけのスナップ、解除、無効なドロップ、ミラー、プレビューと確定位置の一致、端面への挿入を検証。
- 分離時の質量・燃料・電力・線形/角運動量の保存、パーツの世界座標の連続性、側面パーツの所属、未分離の上段エンジンの抑止、段別燃料、上段再点火、リセットを検証。
- 分離のlease制約、発射台での拒否、二重分離防止、分離を含むatomic batchの全件検証を確認。
- 上流PyLoN互換試験: 分離を含む7種類のコマンドを受理し、95テレメトリパケットを上流パーサーで解析。
- `node tests/separation-smoke.js http://127.0.0.1:3002`: 実UDPで離陸 → 分離 → 破片テレメトリ → 上段点火 → 重複指令拒否を確認。終了後は元の機体で組立室へ復帰。
- ブラウザ: RCSの自由配置（高さ0.019・角度103°）、配置済みグループの別タンクへの移動、空中ドロップの拒否、スナップ時の高さ0・角度90°、OFF時の連続位置（高さ−0.025・角度92°）、Undoを確認。
- 2段機体デモ: 数値テストで自動分離・上段点火・高度100 km超えを確認。実ブラウザでもT+65.6秒の下段分離と上段の表示・燃料消費を確認。実時間確認の途中で既存のテレメトリ監視によるデモ停止が一度発生したため、分離と上段点火は上記の実UDP試験でも独立して確認。
