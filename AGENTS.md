# 作業完了時のルール

- このリポジトリで変更を伴う作業を完了したら、適切な確認を実施し、変更をコミットしてリモートへプッシュする。
- ユーザーから別の指示がない限り、コミット・プッシュのたびに確認を求めず実行する。
- リモート未設定、認証エラー、確認の失敗などで完了できない場合は、完了した範囲と理由を報告する。
- 既存の履歴を壊す強制プッシュは行わない。

# デモ実装の独立性（必須）

- デモはAstroForge本体とは別プロセスの外部クライアントとして実装する。本体と通信する経路は公開されたUDPプロトコルだけとする。
- デモのために本体のサーバー・UI・API・物理計算・ライフサイクルへ処理を追加しない。本体からデモをimport・起動・管理しない。
- デモから本体の内部モジュール・HTTP API・保存データ・実行中の内部状態へアクセスしない。デモ用ダッシュボードも独自のサーバーと画面を持つ。
- UDP操作用デモは、機体の形状・段数・構成・パーツの並び順・固定パーツIDに依存させない。送信対象の名前は利用者が指定し、UDP manifestは入力候補としてのみ使う。
- ボタンやスライダーは利用者が指定したUDP指令を送信する。機体構成を推測した対象の切替、自動分離、自動点火、自動姿勢制御を組み込まない。
- デモの検証では本体を起動せずにUDPの送受信を確認できるテストを用意し、デモを追加するための本体変更がないことも確認する。
