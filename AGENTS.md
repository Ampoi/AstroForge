# 作業完了時のルール

- このリポジトリで変更を伴う作業を完了したら、適切な確認を実施し、変更をコミットしてリモートへプッシュする。
- ユーザーから別の指示がない限り、コミット・プッシュのたびに確認を求めず実行する。
- リモート未設定、認証エラー、確認の失敗などで完了できない場合は、完了した範囲と理由を報告する。
- 既存の履歴を壊す強制プッシュは行わない。

# デモ実装の独立性（必須）

- デモはAstroForge本体とは別プロセスの独立したROS2クライアントとして実装する。公開UDPプロトコルを扱うbridgeを介して、ROS2のtopic・serviceで観測と指令をやり取りする。本体との通信経路は公開UDPプロトコルだけとする。
- デモのために本体のサーバー・UI・API・物理計算・ライフサイクルへ処理を追加しない。本体からデモをimport・起動・管理しない。
- デモから本体の内部モジュール・HTTP API・保存データ・実行中の内部状態へアクセスしない。デモ用ダッシュボードも独自のサーバーと画面を持つ。
- 公開UDPからbridge経由で取得したテレメトリ・manifest・アクチュエータ状態に基づく自動制御を許可する。自動点火・段分離・姿勢制御・軌道投入の誘導はデモ側のROS2クライアントへ実装し、独立性を満たす自動制御に例外承認は不要とする。
- 自動制御で使う機体構成・対象名・段の順序・目標軌道は、利用者の設定または公開UDPの観測から取得する。特定機体向けのミッション設定を用意してよいが、本体内部のパーツ配列や非公開の固定IDへ依存しない。manifestは対象の探索・能力確認にも使用できる。
- 手動操作用のボタン・スライダーは利用者が指定した指令を送信する。自動制御は明示的に選択・開始するモードとして提供し、手動操作に暗黙の点火・分離・姿勢制御を追加しない。
- 自動制御は制御権の取得応答を確認してから操縦指令を送る。セッション変更・制御権喪失・観測途絶を検出して指令送信を停止し、指令期限とleaseを管理する。
- デモの検証では本体を起動せずに、模擬UDP相手と実際のbridge・ROS2クライアントによる観測・指令の往復を確認できるテストを用意する。デモを追加するための本体変更がないことも確認する。

# ROS2デモのSpaceROS検証（必須）

- デモの実装・変更時は、SpaceROS環境で対象ROS2パッケージと依存bridgeをビルドし、関連テスト・チェックを実行する。ホストの通常のROS2環境だけでの成功をSpaceROSの検証成功として扱わない。
- 自動制御の判断・状態遷移に加え、ROS2とUDPの往復、制御権・セッション・指令期限、通信途絶時の停止を検証する。軌道投入などのミッション達成は、公開UDP由来のテレメトリと明示した成功条件で確認する。
- 検証に使用したSpaceROSのバージョンまたはコンテナイメージ、実行コマンド、結果を記録する。環境不足やチェック失敗で実行できない場合は、その理由と未検証の範囲を報告し、成功したと記載しない。
